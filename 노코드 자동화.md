# 노코드 자동화 기초: 워크플로우 설계 프로젝트

> 취업컨설팅 신청 접수 자동화를 **Make**, **Zapier** 2가지 도구로 구현하고 비교 분석한 후, 구글앱스크립트도 추가로 구현해본 프로젝트입니다.
 
- **사용 도구** : Make, Zapier, Google Apps Script, Google Forms, Google Sheets, Gmail

---

## 목차

1. 프로젝트 개요
2. 자동화 기본 용어 정리
3. 공통 워크플로우 설계
4. 프로젝트 1-A. Make 구현
5. 프로젝트 1-B. Zapier 구현
6. 참고. Google Apps Script 구현
7. 도구 비교 분석
8. 장단점 정리
9. 트러블슈팅 기록
10. 비용 및 개인정보 처리
11. 결론 및 배운 점
12. 과제 요건 체크리스트

---

## 1. 프로젝트 개요

### 1.1 문제 상황

취업컨설팅 신청을 구글폼으로 받고 있으나, 다음 작업을 **매번 수동으로** 처리해야 했습니다.

- 신청 내용을 관리용 시트로 옮겨 적기
- 신청자가 선택한 **상담 방법(화상 / 온라인)** 을 눈으로 확인해 분류하기
- 상담 방법에 따라 **다른 안내 메일**을 발송하기

신청이 몰리는 시기에는 누락과 오분류가 발생하고, 담당자의 확인 시점에 따라 회신이 지연되는 문제가 있었습니다.

### 1.2 자동화 목표

| 구분 | 자동화 전 | 자동화 후 |
|---|---|---|
| 시트 기록 | 수동 복사·붙여넣기 | 제출 즉시 자동 기록 |
| 상담방법 분류 | 담당자 육안 확인 | 필터/라우터가 자동 분기 |
| 안내 메일 | 개별 수동 작성 | 분기별 템플릿 자동 발송 |
| 처리 소요 | 건당 약 3~5분 | 건당 0분 (무인) |

### 1.3 산출물 구성

- **프로젝트 1** : 동일한 흐름을 Make와 Zapier 두 도구로 각각 구현 후 비교 분석
- **프로젝트 2** : 트리거 1개 + 액션 2개 + 필터/라우터를 포함한 자유 주제 자동화 (본 컨설팅 접수 자동화가 이에 해당)
- **추가 검증** : 코드 기반 자동화(Apps Script)와의 대조 실험

---

## 2. 자동화 기본 용어 정리

| 용어 | 설명 | 본 프로젝트에서의 적용 |
|---|---|---|
| **트리거 (Trigger)** | 워크플로우를 시작시키는 사건 | 구글폼 신규 응답 제출 |
| **액션 (Action)** | 트리거 이후 실행되는 작업 | ① 구글시트 행 추가 ② Gmail 발송 |
| **필터 (Filter)** | 조건을 만족할 때만 다음 단계로 진행 | 상담방법에 `화상` 포함 여부 판정 |
| **라우터 (Router)** | 하나의 흐름을 여러 경로로 분기 | 화상 경로 / 온라인 경로 |
| **매핑 (Mapping)** | 앞 단계 데이터를 뒤 단계 입력값에 연결 | 폼 응답 → 시트 열, 메일 본문 |
| **폴링 (Polling)** | 일정 주기로 변경 사항을 확인하는 방식 | Zapier 무료 15분 주기 |
| **모듈 / 스텝** | 워크플로우를 구성하는 단위 블록 | Make 모듈, Zapier 스텝 |
| **시나리오 / Zap** | 완성된 워크플로우 한 벌 | Make 시나리오, Zapier Zap |

---

## 3. 공통 워크플로우 설계

세 도구 모두 **동일한 논리 구조**를 구현하여 비교의 공정성을 확보했습니다.

```
[트리거] 구글폼 신규 응답
        │
        ▼
[분기 조건] 상담방법 값 판정
        │
   ┌────┴────┐
   ▼         ▼
 "화상"     "온라인"
   │         │
   ▼         ▼
[액션1] 구글시트 행 추가
   │         │
   ▼         ▼
[액션2] Gmail 안내 메일 발송
 [화상] 제목   [온라인] 제목
```

### 3.1 데이터 구조

관리 시트 `취업컨설팅 신청폼 옮김` / 워크시트 `상담신청 폼 옮김`

| 열 | A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|---|
| 항목 | 이름 | 학번 | 지역 | 기수 | 상담방법 | 자료제출 | 희망상담일 |

### 3.2 분기 기준값

구글폼 `상담방법` 항목의 실제 선택지는 다음 두 가지입니다.

| 선택지 원문 | 판정 키워드 |
|---|---|
| `화상(웹엑스링크: ***)` | `화상` |
| `온라인 (카톡 , 이메일 등)` | `온라인` |

> **설계 판단** : 선택지 원문에 URL과 불규칙한 공백이 포함되어 있어, 완전 일치(`Equal to`)가 아닌 **부분 일치(`Contains`)** 로 조건을 설계했습니다. 이 판단이 이후 트러블슈팅의 핵심이 되었습니다.

---

## 4. 프로젝트 1-A. Make 구현

### 4.1 시나리오 구조

Make는 **라우터 모듈 1개**로 두 갈래 분기를 한 화면에서 처리했습니다.

```
Google Forms (Watch Responses)
        │
        ▼
     Router
        ├─[필터: 화상]─→ Google Sheets (Add a Row) ─→ Gmail (Send an email)
        └─[필터: 온라인]→ Google Sheets (Add a Row) ─→ Gmail (Send an email)
```

### 4.2 모듈별 설정

| # | 모듈 | 이벤트 | 주요 설정 |
|---|---|---|---|
| 1 | Google Forms | Watch Responses | 대상 폼: 취업컨설팅 신청폼 |
| 2 | Flow Control | Router | 2개 경로 |
| 3 | Google Sheets | Add a Row | 시트/워크시트 지정, 7개 열 매핑 |
| 4 | Gmail | Send an email | Body type: **Text** |

### 4.3 필터 설정

두 경로 모두 라우터에서 뻗어 나온 연결선에 필터를 설정했습니다.

| 경로 | Label | 좌변 | 연산자 | 우변 |
|---|---|---|---|---|
| 상단 | 화상상담 | `상담방법` | Text operators: **Contains** | `화상` |
| 하단 | 온라인상담 | `상담방법` | Text operators: **Contains** | `온라인` |

### 4.4 메일 본문 매핑

`Body type`을 **Text**로 지정한 뒤, 우측 변수 패널에서 항목을 클릭해 매핑했습니다.

```
화상(웹엑스) 컨설팅 신청이 접수되었습니다.

■ 신청자 : {{신청자 이름}}
■ 학번 : {{학번}}
■ 지역 : {{지역}}
■ 기수 : {{기수}}
■ 상담방법 : {{상담방법}}
■ 자료제출 : {{자료제출}}
■ 희망 상담일 : {{희망상담일}}
```

### 4.5 실행 결과

`Run once` 후 구글폼으로 실제 제출하여 두 경로를 각각 검증했습니다.

| 테스트 | 입력 | 기대 결과 | 실제 결과 |
|---|---|---|---|
| 1 | 상담방법 = 화상 | 상단 경로만 실행 | 통과 |
| 2 | 상담방법 = 온라인 | 하단 경로만 실행 | 통과 |

각 테스트에서 해당 경로의 모듈에만 실행 표시가 나타났고, 반대 경로는 필터에서 차단되어 실행되지 않았습니다.

---

## 5. 프로젝트 1-B. Zapier 구현

### 5.1 설계 제약과 우회 전략

Zapier의 분기 기능인 **Paths는 Professional 플랜 전용**입니다. 따라서 라우터 대신 **Zap을 2개로 분리하고 각각에 필터를 거는 방식**으로 동일한 결과를 구현했습니다.

| 항목 | Zap A | Zap B |
|---|---|---|
| Zap 이름 | `컨설팅_화상상담` | `컨설팅_온라인상담` |
| 1. 트리거 | Google Forms – New Form Response | 동일 |
| 2. 필터 | 상담방법 **Contains** `화상` | 상담방법 **Contains** `온라인` |
| 3. 액션 | Google Sheets – Create Spreadsheet Row | 동일 |
| 4. 액션 | Gmail – Send Email `[화상]` | Gmail – Send Email `[온라인]` |

> Zap B는 Zap A를 **Duplicate** 하여 필터값·제목·본문 첫 줄 3곳만 수정해 생성했습니다. 구축 시간을 절반 이하로 단축했습니다.

### 5.2 스텝별 설정

| # | 앱 | 이벤트 | 주요 설정 |
|---|---|---|---|
| 1 | Google Forms | New Form Response | 대상 폼 선택 |
| 2 | Filter by Zapier | Only continue if… | 상담방법 (Text) Contains |
| 3 | Google Sheets | **Create Spreadsheet Row** | 헤더 자동 인식 후 7개 열 매핑 |
| 4 | Gmail | Send Email | Body Type: **Plain** |

### 5.3 실행 결과

Zap History에서 두 Zap의 처리 결과를 대조했습니다.

| 제출 내용 | 컨설팅_화상상담 | 컨설팅_온라인상담 |
|---|---|---|
| 상담방법 = 화상 | **Success** | **Filtered** |
| 상담방법 = 온라인 | **Filtered** | **Success** |

> `Filtered` 상태는 **필터가 조건 불일치를 정상적으로 걸러냈다**는 의미입니다. 오류가 아니며, 분기가 실제로 작동했음을 증명하는 핵심 근거입니다.

### 5.4 무료 플랜 한계 실측

구축 과정에서 Zapier 무료 플랜의 제약을 직접 확인했습니다.

| 기능 | 무료 플랜 | 본 프로젝트 대응 |
|---|---|---|
| Zap 스텝 수 | 트리거 1 + 액션 1 (2스텝) | 4스텝 필요 → 체험 기간 활용 |
| Filter by Zapier | 사용 불가 (유료) | 체험 기간 활용 |
| Paths (분기) | 사용 불가 (Professional) | **Zap 2개 분리로 우회** |
| 월 태스크 | 100 tasks | 테스트 범위 내 충분 |
| 폴링 주기 | 15분 | 체험 기간 중에는 단축 적용 |

> 신규 가입 시 **14일 프리미엄 체험**이 자동 부여되어 멀티스텝과 필터를 사용할 수 있었습니다. 체험 종료 후에는 동일 구성을 유지할 수 없다는 점이 Make와의 결정적 차이입니다.

---

## 6. 참고. Google Apps Script 구현

동일한 흐름을 **코드 기반**으로도 구현하여, 노코드 도구와의 차이를 실측했습니다.

### 6.1 구현 방식

구글시트 확장 프로그램 → Apps Script에서 스크립트를 작성하고, **설치형 트리거 `onFormSubmit`** 을 등록하여 폼 제출 시 자동 실행되도록 설정했습니다.

```javascript
function onFormSubmit(e) {
  const v = e.values;              // 폼 응답 배열
  const 이름 = v[1];
  const 학번 = v[2];
  const 지역 = v[3];
  const 기수 = v[4];
  const 상담방법 = v[5];
  const 자료제출 = v[6];
  const 희망상담일 = v[7];

  // 액션 1 : 관리 시트에 행 추가
  const sheet = SpreadsheetApp
    .openById('SPREADSHEET_ID')
    .getSheetByName('상담신청 폼 옮김');
  sheet.appendRow([이름, 학번, 지역, 기수, 상담방법, 자료제출, 희망상담일]);

  // 분기 조건 : 노코드의 필터/라우터에 해당
  const isVideo = 상담방법.indexOf('화상') !== -1;
  const 제목 = isVideo ? '[화상] 컨설팅 신청접수' : '[온라인] 컨설팅 신청접수';
  const 첫줄 = isVideo
    ? '화상(웹엑스) 컨설팅 신청이 접수되었습니다.'
    : '온라인(카톡) 컨설팅 신청이 접수되었습니다.';

  // 액션 2 : Gmail 발송
  const 본문 =
    첫줄 + '\n\n' +
    '■ 신청자 : ' + 이름 + '\n' +
    '■ 학번 : ' + 학번 + '\n' +
    '■ 지역 : ' + 지역 + '\n' +
    '■ 기수 : ' + 기수 + '\n' +
    '■ 상담방법 : ' + 상담방법 + '\n' +
    '■ 자료제출 : ' + 자료제출 + '\n' +
    '■ 희망 상담일 : ' + 희망상담일;

  GmailApp.sendEmail('담당자메일', 제목, 본문);
}
```

### 6.2 실행 결과

| 검증 항목 | 결과 |
|---|---|
| 폼 제출 시 자동 실행 | 정상 |
| 구글시트 행 추가 | 정상 |
| 조건 분기 (화상/온라인) | 정상 |
| Gmail 발송 | 정상 |

### 6.3 노코드 도구와의 구조 대응

| 노코드 개념 | Apps Script 구현체 |
|---|---|
| 트리거 | 설치형 트리거 `onFormSubmit` |
| 필터 / 라우터 | `if` 조건문 (`indexOf('화상')`) |
| 액션 (시트) | `appendRow()` |
| 액션 (메일) | `GmailApp.sendEmail()` |
| 매핑 | 배열 인덱스 `e.values[n]` |

> **Apps Script는 노코드 도구가 아닙니다.** JavaScript 작성·수정 역량이 필요하므로 코드 기반(로우코드) 자동화로 분류됩니다. 본 보고서에서는 비교 대조군으로만 다루며, 프로젝트 1의 정식 비교 대상은 Make와 Zapier입니다.

---

## 7. 도구 비교 분석

### 7.1 종합 비교표 (비교 항목 8개)

| 비교 항목 | Make | Zapier | Apps Script |
|---|---|---|---|
| **① 분기 처리 방식** | 라우터 모듈 1개로 무료 처리 | Paths 유료 → **Zap 2개로 분리 우회** | `if` 조건문, 제약 없음 |
| **② 무료 플랜 범위** | 월 1,000 operations, 멀티스텝 자유 | 월 100 tasks, **2스텝 제한** (필터 유료) | 완전 무료, 일일 쿼터만 존재 |
| **③ 실행 주기** | 최소 1분 | 무료 15분 / 유료 1~2분 | 제출 즉시 (이벤트 기반) |
| **④ 설계·UI 방식** | 캔버스형 시각적 연결 | 세로 리스트형 순차 배치 | 텍스트 에디터 |
| **⑤ 진입 장벽** | 중 (모듈·변수 개념 학습 필요) | 낮음 (질문에 답하듯 진행) | 높음 (JavaScript 필수) |
| **⑥ 유지보수 주체** | 비개발자 가능 | 비개발자 가능 | **개발 가능자 필요** |
| **⑦ 외부 서비스 확장성** | 2,000여 개 앱 연동 | 8,000여 개 앱 연동 | 구글 생태계 중심, 외부는 직접 API 호출 |
| **⑧ 오류 처리·디버깅** | 모듈별 번들 확인, 에러 핸들러 | Zap History 상태 표시 | 실행 로그, `try/catch` 직접 작성 |

### 7.2 동일 작업 구축 소요 시간 실측

| 도구 | 최초 구축 | 두 번째 분기 추가 | 합계 |
|---|---|---|---|
| Make | 약 60분 | 약 10분 (경로 복제) | **약 70분** |
| Zapier | 약 35분 | 약 5분 (Zap Duplicate) | **약 40분** |
| Apps Script | 약 40분 | 약 2분 (`if` 한 줄) | **약 42분** |

> Make는 개념 학습에 시간이 더 들었으나, 한 화면에서 전체 흐름을 조망할 수 있어 수정 시 효율이 높았습니다. Zapier는 초기 진입은 가장 빨랐지만 분기를 Zap 2개로 나누면서 **관리 대상이 두 배**가 되었습니다.

### 7.3 분기 구현 방식의 근본적 차이

```
[Make]              [Zapier]                 [Apps Script]
  트리거               트리거    트리거            트리거
    │                  │        │                 │
  라우터               필터     필터            if 조건문
   ├─화상              화상     온라인            ├─화상
   └─온라인             │        │                └─온라인
                      액션     액션
  워크플로우 1개      워크플로우 2개           스크립트 1개
```

**핵심 인사이트** : Zapier는 트리거가 두 Zap에서 **중복 실행**되므로, 신청 1건당 태스크 소비가 Make보다 많아집니다. 분기가 늘어날수록 이 격차는 선형적으로 커집니다.

---

## 8. 장단점 정리

### 8.1 Make

**장점**
- 라우터를 무료 플랜에서 제한 없이 사용할 수 있어 복잡한 분기에 유리
- 캔버스형 UI로 전체 흐름을 한눈에 파악 가능, 문서화·설명에 유리
- 모듈별 실행 번들을 직접 열어볼 수 있어 원인 추적이 정밀함
- 무료 operations 한도가 Zapier의 10배 수준

**단점**
- 변수 매핑 문법이 초보자에게 직관적이지 않음
- 모듈이 늘어나면 캔버스가 복잡해져 가독성이 떨어짐
- 한국어 학습 자료가 Zapier에 비해 적음

### 8.2 Zapier

**장점**
- 질문에 답하듯 진행하는 UI로 최초 진입 장벽이 가장 낮음
- 연동 가능한 앱 수가 가장 많음
- Duplicate 기능으로 유사 워크플로우 복제가 매우 빠름
- Zap History의 `Success` / `Filtered` 표시가 직관적

**단점**
- **무료 플랜에서 필터와 멀티스텝을 쓸 수 없어** 사실상 실무 활용이 어려움
- 분기(Paths)가 고가 플랜 전용이라 Zap 분리라는 비효율을 감수해야 함
- Zap이 분리되면 수정 사항을 양쪽에 각각 반영해야 해 누락 위험 존재
- 무료 폴링 15분으로 실시간성이 떨어짐

### 8.3 Google Apps Script

**장점**
- 완전 무료이며 실행 횟수·스텝 수 제약이 사실상 없음
- 이벤트 기반이라 폴링 지연 없이 즉시 실행
- 구글 서비스 내부 제어가 세밀하게 가능
- 분기·반복 등 복잡한 로직을 코드 한 줄로 처리

**단점**
- **JavaScript 작성 및 수정 역량이 필수** → 담당자 교체 시 인수인계 리스크
- 시각적 흐름도가 없어 비개발자와의 커뮤니케이션이 어려움
- 구글 생태계 밖 서비스 연동 시 API를 직접 구현해야 함
- 오류 발생 시 로그를 직접 해석해야 함

### 8.4 상황별 권장

| 상황 | 권장 도구 | 근거 |
|---|---|---|
| 분기가 많은 무료 자동화 | **Make** | 라우터가 무료 |
| 다양한 외부 SaaS 연동 | **Zapier** | 최대 앱 커버리지 |
| 구글 서비스 내부에서만 완결 | **Apps Script** | 무료·즉시 실행 |
| 비개발자가 직접 유지보수 | **Make / Zapier** | 코드 불필요 |
| 실시간성이 중요 | **Apps Script** | 폴링 지연 없음 |

---

## 9. 트러블슈팅 기록

실제 구축 과정에서 발생한 문제와 해결 과정입니다.

### 문제 1. 필터가 항상 불일치 처리됨

| 항목 | 내용 |
|---|---|
| **증상** | 화상 신청을 제출해도 해당 경로가 실행되지 않음 |
| **원인** | 조건을 `Equal to` + `화상`으로 설정했으나, 실제 저장값은 괄호와 URL이 포함된 전체 문자열이었음 |
| **해결** | 연산자를 **`Contains`** 로 변경, 비교값은 `화상` 두 글자만 사용 |
| **교훈** | 객관식 선택지에 URL·괄호·공백이 포함되면 완전 일치는 실패한다. **조건 설계 전 실제 저장값을 반드시 먼저 확인**할 것 |

### 문제 2. 메일이 발송되지만 본문이 비어 있음

| 항목 | 내용 |
|---|---|
| **증상** | Gmail 모듈이 정상 실행되는데 수신 메일 본문이 공백 |
| **원인** | Make의 `Body type`이 `Collection of contents`로 지정되어 `Body contents`가 비어 있었음 |
| **해결** | `Body type`을 **`Text`** 로 변경 후 본문 매핑 (Zapier는 `Plain` 선택) |
| **교훈** | 모듈 실행 성공이 곧 결과 정상을 의미하지 않는다. **최종 산출물까지 눈으로 확인**할 것 |

### 문제 3. 시트에 행이 추가되지 않고 서식만 변경됨

| 항목 | 내용 |
|---|---|
| **증상** | Zapier 액션 설정 시 `Frozen Rows Count` 같은 엉뚱한 입력칸이 표시됨 |
| **원인** | 액션 이벤트를 `Change Sheet Properties`(시트 속성 변경)로 잘못 선택 |
| **해결** | **`Create Spreadsheet Row`** 로 변경 → 시트 헤더명 입력칸이 정상 표시됨 |
| **교훈** | 이름이 유사한 액션이 많다. **설정 화면에 뜨는 입력 항목이 의도와 맞는지**로 검증할 것 |

### 문제 4. 메일이 예상보다 많이 발송된 것으로 오인

| 항목 | 내용 |
|---|---|
| **증상** | 테스트 후 메일이 4통 수신되어 분기 오류로 판단 |
| **원인 분석** | 실행 이력을 대조한 결과 **제출 건수 자체가 4건**(화상 2건 + 온라인 2건)이었음. 각 건당 1통씩 정상 발송된 것 |
| **해결** | 오류 아님. 이후 테스트는 **시트를 비우고 1건씩 제출**하여 인과관계를 명확히 확인 |
| **교훈** | 결과 수치만으로 판단하지 말고 **실행 이력에서 개별 건을 열어 대조**할 것. 테스트는 변수를 하나씩만 바꿔야 한다 |

### 문제 5. Zapier 무료 플랜에서 필터를 추가할 수 없음

| 항목 | 내용 |
|---|---|
| **증상** | Filter 스텝이 유료 기능으로 잠김 |
| **원인** | 무료 플랜은 트리거 1 + 액션 1의 2스텝만 지원 |
| **해결** | 신규 가입 시 부여되는 **14일 프리미엄 체험** 기간 내에 구축·검증 완료. 분기는 Paths 대신 Zap 2개 분리로 대체 |
| **교훈** | 도구 선정 시 기능 유무보다 **무료 플랜에서 그 기능이 열려 있는지**가 실질적 제약이다 |

---

## 10. 비용 및 개인정보 처리

### 10.1 비용

| 도구 | 사용 플랜 | 실제 비용 | 지속 가능성 |
|---|---|---|---|
| Make | Free | 0원 | 월 1,000 ops 내에서 계속 사용 가능 |
| Zapier | Free (14일 체험) | 0원 | **체험 종료 시 현 구성 유지 불가** |
| Apps Script | 무료 | 0원 | 제약 없이 계속 사용 가능 |

> 본 프로젝트는 전 과정을 **추가 비용 없이** 완료했습니다. 다만 Zapier 구성을 실운영하려면 유료 플랜 전환이 필요합니다.

### 10.2 개인정보 처리

- 본 문서에는 실제 신청자 데이터를 포함하지 않았습니다
- 이메일 주소, 성명, 학번은 모두 임의값 또는 마스킹 처리했습니다
- 스프레드시트 ID와 화상회의 접속 링크는 문서에서 제외했습니다
- 테스트는 전용 더미 데이터로만 수행했습니다

---

## 11. 결론 및 배운 점

### 11.1 결론

동일한 자동화를 세 가지 방식으로 구현한 결과, **"어떤 도구가 우수한가"보다 "어떤 제약 조건에 놓여 있는가"가 선택을 결정한다**는 점을 확인했습니다.

- 분기가 필요한 무료 자동화라면 **Make**가 유일한 현실적 선택지였습니다. Zapier는 같은 기능이 유료 장벽 뒤에 있었습니다.
- 연동 대상이 구글 서비스로 한정된다면 **Apps Script**가 비용·속도 면에서 압도적이었습니다. 다만 유지보수 인력 제약이 명확한 대가로 따라왔습니다.
- **노코드의 본질적 가치는 결과물이 아니라 유지보수 주체에 있습니다.** 세 도구의 최종 산출물은 완전히 동일했으나, 조건 하나를 수정할 때 Make·Zapier는 드롭다운 조작으로 끝난 반면 Apps Script는 코드 수정이 필요했습니다.

### 11.2 배운 점

1. **조건 설계는 실제 데이터 확인이 선행되어야 한다.** `Equal to` 실패 사례에서 가장 크게 체감했습니다.
2. **실행 성공과 결과 정상은 별개다.** 실행 표시가 정상이어도 본문이 빈 메일이 나갈 수 있었습니다.
3. **무료 플랜의 경계가 곧 설계 제약이다.** Zapier의 Paths 제한이 아키텍처 자체를 바꾸게 만들었습니다.
4. **테스트는 변수를 하나만 바꿔야 한다.** 4통 오인 사례는 여러 건을 한꺼번에 제출해 인과관계가 흐려진 탓이었습니다.
5. **분기를 워크플로우 분리로 우회하면 관리 비용이 배로 증가한다.** 수정 누락 위험이 실제로 존재했습니다.

### 11.3 향후 개선 과제

- **중복 신청 처리** : 동일 날짜 + 동일 학번 재신청 시 기존 행을 갱신하는 로직 (Search Rows + Array Aggregator + Update a Row 구조로 설계 중)
- **구글 캘린더 연동** : 확정된 상담 일정을 담당자 캘린더에 자동 등록
- **오류 알림** : 실행 실패 시 담당자에게 별도 통보하는 에러 핸들러 추가

---

## 12. 과제 요건 체크리스트

| # | 요건 | 충족 | 근거 위치 |
|---|---|:--:|---|
| 1 | 동일 흐름을 2개 이상 도구로 구현 | 충족 | 4장(Make), 5장(Zapier), 6장(Apps Script) |
| 2 | 트리거 1개 이상 | 충족 | 구글폼 신규 응답 |
| 3 | 액션 2개 이상 | 충족 | 시트 행 추가 + Gmail 발송 |
| 4 | 필터 또는 라우터 포함 | 충족 | Make 라우터 + 필터 2개 / Zapier 필터 2개 |
| 5 | 실제 동작하는 워크플로우 | 충족 | 4.5 및 5.3 실행 결과 |
| 6 | 실행 증빙 | 충족 | 화면 캡처 별도 제출 |
| 7 | 비교 항목 5개 이상 | 충족 | 7.1 종합 비교표 (**8개 항목**) |
| 8 | 장단점 정리 | 충족 | 8장 |
| 9 | 트러블슈팅 기록 | 충족 | 9장 (5건) |
| 10 | 결론 도출 | 충족 | 11장 |
